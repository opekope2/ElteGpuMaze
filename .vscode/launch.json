{
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Build & Debug",
            "target": "${workspaceRoot}/bin/maze",
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText",
            "preLaunchTask": "Build"
        },
        {
            "type": "gdb",
            "request": "launch",
            "name": "Debug",
            "target": "${workspaceRoot}/bin/maze",
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText"
        }
    ]
}