{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "g++ -g -o ${workspaceRoot}/bin/main ${workspaceRoot}/main.cpp"
        }
    ]
}
